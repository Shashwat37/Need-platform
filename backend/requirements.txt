# Python packages needed by the NEED backend.
# Versions are pinned so the project installs the same way on every laptop.
# Install with:  pip install -r requirements.txt

Flask==3.0.3
Flask-Cors==4.0.1
Flask-SQLAlchemy==3.1.1
python-dotenv==1.0.1
scikit-learn>=1.5.0
requests>=2.31.0
psycopg2-binary>=2.9.9
gunicorn>=22.0.0
